name: Keep Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # 每 10 分鐘（Render 免費版 15 分鐘沒流量會休眠，用 10 分鐘保留緩衝）
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping server
        run: curl -s "${{ secrets.RENDER_APP_URL }}/" || true
