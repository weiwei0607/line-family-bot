name: Keep Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # 每 10 分鐘
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping server
        run: curl -s "${{ secrets.RENDER_URL }}/health" || true
